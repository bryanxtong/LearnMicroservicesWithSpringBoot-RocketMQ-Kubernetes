apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-spanmetrics
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Demo"
data:
  spanmetrics-dashboard.json: |
{{ .Files.Get "dashboards/spanmetrics-dashboard.json" | indent 4 }}

apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-demo
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Demo"
data:
  demo-dashboard.json: |
{{ .Files.Get "dashboards/demo-dashboard.json" | indent 4 }}

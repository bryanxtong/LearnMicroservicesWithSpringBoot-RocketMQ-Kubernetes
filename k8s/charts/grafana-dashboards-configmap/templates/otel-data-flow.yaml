apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-otel-data-flow
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Demo"
data:
  opentelemetry-collector-data-flow.json: |
{{ .Files.Get "dashboards/opentelemetry-collector-data-flow.json" | indent 4 }}
